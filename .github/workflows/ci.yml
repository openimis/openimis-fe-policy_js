name: Run Module Tests

on:
  push:
    branches:
      - develop
      - 'feature*'
      - 'release/**'
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - develop
      - 'feature*'
      - 'release/**'

jobs:
  run-tests:
    uses: Y-Note-SAS/openimis-fe_js/.github/workflows/ci-module.yml@feature-34644
    with:
      module-path: ./openimis-fe-policy_js
      repo: ${{ github.repository }}
